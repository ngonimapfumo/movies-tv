<resources>
    <!-- Base application theme. -->
    <style name="Theme.MovieDB" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Primary brand color. -->
       <!-- <item name="android:windowFullscreen">true</item>-->
        <item name="fontFamily">@font/kumb</item>
        <item name="colorPrimary">#1976D2</item>
        <item name="colorPrimaryVariant">#303F9F</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#303F9F</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->

        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/anim</item>
        <item name="android:windowExitTransition">@transition/anim</item>

    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieDB" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>